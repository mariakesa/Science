What it looks like now is not one single clean loop, but something more like:

a shared global scaffold
with several stacked sheets / ribbons
and syllables occupying different preferred strata on that scaffold

Reminds me of this https://winnieyangwannan.github.io/RippleTagging/

The visualization is sensitive to initialization. Have to run the experiment multiple times to find a good seed. 
--> Note to self: introduce a seed to control the randomness. 